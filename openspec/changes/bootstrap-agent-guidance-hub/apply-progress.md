# Apply Progress: bootstrap-agent-guidance-hub

**Change**: bootstrap-agent-guidance-hub  
**Mode**: Standard (strict_tdd: false)  
**Delivery**: auto-chain / stacked-to-main  
**Work unit**: PR 1 — Scaffold and test runner  
**Updated**: 2026-05-27

## Completed Tasks

- [x] 1.1 Create `pyproject.toml`, `agh/__init__.py`, `agh/server/app.py`, `agh/cli/main.py`, and `tests/` with FastAPI, Typer, pytest dependencies and `agh` entrypoint.
- [x] 1.2 Add smoke tests in `tests/test_scaffold.py` for `GET /api/v1/health`, `agh --help`, log file creation, and default port constant `8912`.
- [x] 1.3 Implement health route, Typer help, basic stdout/file logging, `Dockerfile` health check; `python -m pytest` passes.
- [x] 1.4 Update `openspec/config.yaml` `apply.test_command`, `verify.test_command`, and testing runner fields to `python -m pytest`.

## Files Changed

| File | Action | Notes |
|------|--------|-------|
| `pyproject.toml` | Created | Package metadata, deps, `agh` console script |
| `README.md` | Created | Minimal dev/server docs including venv setup and local data-dir behavior |
| `Dockerfile` | Created | uvicorn `:8912`, HEALTHCHECK on `/api/v1/health` |
| `agh/__init__.py` | Created | Version |
| `agh/server/__init__.py` | Created | Exports `create_app`, `DEFAULT_PORT` |
| `agh/server/app.py` | Created | Health route, logging, `DEFAULT_PORT=8912`; review fix makes local default writable and logging reload-idempotent |
| `agh/cli/__init__.py` | Created | Package marker |
| `agh/cli/main.py` | Created | Typer CLI with `--help` |
| `tests/conftest.py` | Created | Sets `AGH_DATA_DIR` before imports and cleans temp data dir at session finish |
| `tests/test_scaffold.py` | Created | Four smoke tests |
| `openspec/config.yaml` | Modified | Verified `python -m pytest` commands |
| `openspec/changes/bootstrap-agent-guidance-hub/tasks.md` | Modified | Marked 1.1–1.4 complete |

## Validation

```text
cd /home/gtesta/Projects/aictx && . .venv/bin/activate && python -m pytest -v
# 4 passed

unset AGH_DATA_DIR; .venv/bin/python -c "import agh.server.app; print('import ok')"
# import ok

.venv/bin/python -m pytest -q
# 4 passed in 0.25s
```

## Deviations from Design

- Added `tests/conftest.py` to set `AGH_DATA_DIR` before module import; without it, `app = create_app()` at import time failed on `/data` (permission denied) during test collection.

## Issues Found / Review Fixes

- Initial `pip install` required a venv (`.venv` created locally; not committed per scope).
- Fresh review found local import failed when `AGH_DATA_DIR` was unset because `/data` was not writable. Fixed by defaulting local development to `.agh-data/` while Docker still sets `AGH_DATA_DIR=/data`.
- Fresh review found generated bytecode/cache risk. Fixed `.gitignore` and removed generated caches from the candidate diff.
- Fresh review noted duplicate logging handlers on module reload. Fixed AGH-managed handler cleanup.

## Remaining Tasks

Phase 2+ unchanged — see `tasks.md` (2.1 onward).

## Workload / PR Boundary

- **Mode**: stacked PR slice #1 → `main`
- **Boundary**: Greenfield scaffold through pytest smoke + Dockerfile health; no auth/DB/API beyond health
- **Status**: 4/4 Phase 1 tasks complete; ready for verify and PR 2 apply
