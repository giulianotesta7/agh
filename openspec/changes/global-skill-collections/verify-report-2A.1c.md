Focused hardening slice for issue #97 before PR 2A.2. Scope: `agh/cli/global_skills.py`, `tests/test_global_skills.py`, and SDD artifacts only.
Evidence: native skill paths and AGH state lock/cache paths reject symlinked existing ancestors, including `.config/opencode` and `$XDG_STATE_HOME/agh`; update rollback restores previous target/cache content; rollback restore failures now surface `GlobalSkillError` with manual recovery context; prior tampered-target, lock-before-delete, and malformed-lock protections remain covered.
Commands: `git diff --shortstat feat/global-skill-collections...HEAD` → 5 files changed, 363 insertions(+), 37 deletions(-); `git diff --check feat/global-skill-collections...HEAD` → passed; `uv run pytest tests/test_global_skills.py -q` → 49 passed; `uv run pytest -q` → 441 passed, 1 skipped; `uv run ruff check agh/cli/global_skills.py tests/test_global_skills.py` → All checks passed; `uv run ruff format --check agh/cli/global_skills.py tests/test_global_skills.py` → 2 files already formatted; `uv run --with pyright pyright agh tests` → 0 errors, 0 warnings, 0 informations.
Remaining findings: none for PR 2A.1c. PR 2A.2 CLI commands remain intentionally out of scope.
